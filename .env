# ─────────────────────────────────────────────
# 엔터핑 이메일 설정
# 이 파일을 .env 로 저장
# ─────────────────────────────────────────────

# Gmail SMTP 설정
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# 발신에 사용할 Gmail 주소
SMTP_USER=kty98116@gmail.com

# Gmail 앱 비밀번호 (Google 계정 → 보안 → 2단계 인증 → 앱 비밀번호에서 생성)
# 일반 Gmail 비밀번호가 아닌 앱 전용 비밀번호를 입력하세요
SMTP_PASSWORD=fjosuxsmhixngslt

# 이메일 발신자 이름
SENDER_NAME=엔터핑

# MySQL 데이터베이스 연결 URL
# 형식: mysql+pymysql://유저이름:비밀번호@호스트:포트/DB이름
DATABASE_URL=mysql+pymysql://root:dowon12358@localhost:3306/enterping_db
